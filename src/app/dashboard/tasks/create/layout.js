import { appConfig } from "@/config/app";

export const metadata = {
  title: `Create | Tasks or Events | Dashboard | ${appConfig.title}`,
};

export default function CreateTaskLayout({ children }) {
  return children;
}
